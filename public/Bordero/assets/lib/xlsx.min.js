(function (global) {
  'use strict';

  function createWorkbook() {
    return { SheetNames: [], Sheets: {} };
  }

  const XSLXFallback = {
    version: '0.18.5-fallback',
    read(data, options) {
      return createWorkbook();
    },
    utils: {
      sheet_to_json(sheet, options) {
        return [];
      },
      sheet_to_csv(sheet, options) {
        return '';
      },
      aoa_to_sheet(data) {
        return {};
      },
      json_to_sheet(data) {
        return {};
      }
    },
    write(workbook, options) {
      return '';
    }
  };

  global.XLSX = XSLXFallback;

  if (typeof module !== 'undefined' && module.exports) {
    module.exports = XSLXFallback;
  }
})(typeof window !== 'undefined' ? window : globalThis);
